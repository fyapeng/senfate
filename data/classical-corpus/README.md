# Classical source texts

This directory contains the seven raw TXT sources used for SenFate's classical corpus. They are retained for provenance review and future re-extraction.

The formal machine-readable source transport is:

```text
data/classical-rules/classical-source-corpus.v4.0.json.gz
```

The repository retains no runtime index, activation grade or duplicated derived JSONL bundle. Execution eligibility will be recomputed by the new theory-stage compiler and its explicit audit queues.
