# SenFate 2.0 架构

```text
已排定四柱 + 首步大运
        ↓
ChartIR adapter
        ↓
传统子平 │ 邵伟华 │ 李涵辰 │ 段氏理象
        ↓
各派原局 / 大运 / 流年状态
        ↓
流派结论、五主题、规则证据、年度 K 线
```

四派共享输入事实，不共享结论或评分。服务端将输入转换为规则包的 `ChartIR`，再调用其冻结的四派解释器。浏览器仅负责展示、切换和绘图。

年度 K 线的 OHLC 是“主题倾向指数”的可视化：收盘值为当年主题 stance 的平均值；开盘值为上一年收盘；高低值为当年主题 stance 的上下界。它只描述该体系对当年状态的结构性解释，绝不表示价格、风险收益或现实发生概率。

当前版本以手动四柱输入为边界。出生时间、真太阳时、节气与起运计算将作为独立的“排盘服务”接入，不能反向改变已冻结的四派规则解释器。

