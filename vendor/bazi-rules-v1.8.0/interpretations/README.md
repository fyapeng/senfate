# 解释与中性输出

W07 已冻结完整中性输出策略：

- `neutral_output_policy.json`：比较、安全、来源链和输出优先级总合同；
- `W07_LANGUAGE_TRANSFORMS.json`：风险类别、阻断和中性化规则；
- `W07_OUTPUT_TEMPLATES.json`：七种比较状态的用户层模板。

默认输出不包含伤害性原文引语。健康、死亡与犯罪类具体断语阻断；财务、地位、关系和人格类内容转换为中性主题。`unknown`、`contested`、`mixed` 和 `incommensurable` 必须保留。
